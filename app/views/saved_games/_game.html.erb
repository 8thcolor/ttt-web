<table class="game">
  <% (0..2).each do |x| %>
  <tr>
    <% (0..2).each do |y| %>
      <td>
        <% if @game.at(x,y) == 0 && !@game.finish? %>
          <%= link_to ' ', play_saved_game_path(x: x, y: y), method: :post, id: @saved_game.id %>
        <% else %>
        <%= @game.show_at(x,y) %>
        <% end %>
      </td>
    <% end %>
  </tr>
  <% end %>
</table>